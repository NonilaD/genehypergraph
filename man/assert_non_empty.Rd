% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_utils.R
\name{assert_non_empty}
\alias{assert_non_empty}
\title{Assert that a data.table is non-empty}
\usage{
assert_non_empty(dt, name)
}
\arguments{
\item{dt}{[EN] Object to check (expected to be a \code{data.table}).\cr
[ESP] Objeto a comprobar (se espera un \code{data.table}).}

\item{name}{[EN] Dataset name used in the error message.\cr
[ESP] Nombre del dataset usado en el mensaje de error.}
}
\value{
[EN] Invisible \code{TRUE} if the check passes.\cr
[ESP] Invisible \code{TRUE} si la comprobacion pasa.
}
\description{
[EN] Throws an informative \code{stop()} if \code{dt} is \code{NULL} or has zero rows.
}
\details{
[ESP] Lanza un \code{stop()} informativo si \code{dt} es \code{NULL} o tiene cero filas.
}
\examples{
assert_non_empty(mtcars, "mtcars")
}
