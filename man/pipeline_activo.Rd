% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_config.R
\name{pipeline_activo}
\alias{pipeline_activo}
\title{Comprobar si un paso del pipeline está activo}
\usage{
pipeline_activo(config, paso)
}
\arguments{
\item{config}{Lista de configuración devuelta por \code{\link[=load_config]{load_config()}}.}

\item{paso}{Nombre del paso tal como aparece en la sección \code{pipeline} del
YAML, p. ej. \code{"analysis_latent"} o \code{"results_visualization"}.}
}
\value{
\code{TRUE} si el paso está activo, \code{FALSE} en caso contrario.
}
\description{
Wrapper sobre \code{\link[=get_config]{get_config()}} para consultar \verb{pipeline.<paso>}. Funciona
tanto si el valor es un escalar lógico (\code{true}) como si es una lista con
el campo \code{activar} (p. ej. \code{analysis_integration}).
}
\examples{
config <- load_config()
pipeline_activo(config, "analysis_communities")
pipeline_activo(config, "analysis_integration")
pipeline_activo(config, "analysis_latent")
}
