% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_utils.R
\name{gh_checkpoint_save}
\alias{gh_checkpoint_save}
\title{Save a trait result to the run checkpoint}
\usage{
gh_checkpoint_save(config, trait_id, status, msg = NULL)
}
\arguments{
\item{config}{[EN] Configuration list returned by \code{\link[=load_config]{load_config()}}.\cr
[ESP] Lista de configuracion devuelta por \code{\link[=load_config]{load_config()}}.}

\item{trait_id}{[EN] EFO/MONDO identifier of the trait.\cr
[ESP] Identificador EFO/MONDO del trait.}

\item{status}{[EN] \code{"ok"} or \code{"error"}.\cr
[ESP] \code{"ok"} o \code{"error"}.}

\item{msg}{[EN] Optional message (e.g. error text).\cr
[ESP] Mensaje opcional (p.ej. texto del error).}
}
\value{
[EN] Invisible \code{NULL}.\cr
[ESP] Invisible \code{NULL}.
}
\description{
[EN] Appends an entry for \code{trait_id} to
\code{outputs/{run_id}/global/checkpoint.yaml}. If the file does not exist it
is created. Enables resuming interrupted runs by skipping already-completed
traits.
}
\details{
[ESP] Añade una entrada para \code{trait_id} en
\code{outputs/{run_id}/global/checkpoint.yaml}. Si el archivo no existe lo crea.
Permite reanudar ejecuciones interrumpidas saltando los traits ya
completados.
}
\examples{
\dontrun{
config <- load_config()
gh_checkpoint_save(config, "EFO_0001645", "ok")
gh_checkpoint_save(config, "EFO_0000182", "error", msg = "empty dataset")
}
}
