% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_config.R
\name{pipeline_active}
\alias{pipeline_active}
\title{Check if a pipeline step is active}
\usage{
pipeline_active(config, step)
}
\arguments{
\item{config}{[EN] Configuration list returned by \code{\link[=load_config]{load_config()}}.\cr
[ESP] Lista de configuracion devuelta por \code{\link[=load_config]{load_config()}}.}

\item{step}{[EN] Name of the step as it appears in the \code{pipeline} section
of the YAML, e.g. \code{"analysis_latent"} or \code{"results_visualization"}.\cr
[ESP] Nombre del paso tal como aparece en la seccion \code{pipeline} del
YAML, p. ej. \code{"analysis_latent"} o \code{"results_visualization"}.}
}
\value{
[EN] \code{TRUE} if the step is active, \code{FALSE} otherwise.\cr
[ESP] \code{TRUE} si el paso esta activo, \code{FALSE} en caso contrario.
}
\description{
[EN] Wrapper over \code{\link[=get_config]{get_config()}} to query \verb{pipeline.<step>}. Works
whether the value is a scalar logical (\code{true}) or a list with an \code{activar}
field (e.g. \code{analysis_integration}).
}
\details{
[ESP] Wrapper sobre \code{\link[=get_config]{get_config()}} para consultar \verb{pipeline.<paso>}.
Funciona tanto si el valor es un escalar logico (\code{true}) como si es una
lista con el campo \code{activar} (p. ej. \code{analysis_integration}).
}
\examples{
config <- load_config()
pipeline_active(config, "analysis_communities")
pipeline_active(config, "analysis_integration")
pipeline_active(config, "analysis_latent")
}
