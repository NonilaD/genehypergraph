% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_harmonize.R
\name{harmonize_interactome}
\alias{harmonize_interactome}
\title{Filter and normalise interactome}
\usage{
harmonize_interactome(config, dt, genes)
}
\arguments{
\item{config}{[EN] Configuration list created by \code{\link[=load_config]{load_config()}}.\cr
[ESP] Lista de configuracion creada por \code{\link[=load_config]{load_config()}}.}

\item{dt}{[EN] Validated interactome \code{data.table}.\cr
[ESP] \code{data.table} validado de interactoma.}

\item{genes}{[EN] Harmonised gene \code{data.table} (keyed on \code{id}).\cr
[ESP] \code{data.table} armonizado de genes (con clave en \code{id}).}
}
\value{
[EN] Filtered \code{data.table} without duplicates.\cr
[ESP] \code{data.table} filtrado y sin duplicados.
}
\description{
[EN] Filters by minimum score (config), retains only human-human
interactions (both targetA and targetB in the gene list), and removes
self-interactions and duplicates.
}
\details{
[ESP] Filtra por score minimo (config), retiene solo interacciones
entre genes human-human (ambos targetA y targetB en lista de genes), y
elimina auto-interacciones y duplicados.
}
\examples{
\dontrun{
config <- load_config()
genes  <- harmonize_genes(config, validate_genes(config, read_genes(config)))
ppi    <- harmonize_interactome(
  config,
  validate_interactome(config, read_interactome(config)),
  genes
)
}
}
