% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_utils.R
\name{gh_checkpoint_load}
\alias{gh_checkpoint_load}
\title{Load completed trait IDs from the run checkpoint}
\usage{
gh_checkpoint_load(config)
}
\arguments{
\item{config}{[EN] Configuration list returned by \code{\link[=load_config]{load_config()}}.\cr
[ESP] Lista de configuracion devuelta por \code{\link[=load_config]{load_config()}}.}
}
\value{
[EN] Character vector of completed trait IDs (empty if no
checkpoint exists).\cr
[ESP] Vector de character con los trait_id completados (vacio si no
existe checkpoint).
}
\description{
[EN] Reads the checkpoint file of the current run and returns a character
vector of trait IDs that completed with \code{status == "ok"}.
}
\details{
[ESP] Lee el archivo de checkpoint de la ejecucion actual y devuelve un
vector de character con los trait_id que completaron con \code{status == "ok"}.
}
\examples{
\dontrun{
config <- load_config()
done   <- gh_checkpoint_load(config)
}
}
