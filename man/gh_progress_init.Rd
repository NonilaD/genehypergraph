% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_utils.R
\name{gh_progress_init}
\alias{gh_progress_init}
\title{Create a trait progress bar}
\usage{
gh_progress_init(n_total, name = "trait")
}
\arguments{
\item{n_total}{[EN] Total number of units (traits).\cr
[ESP] Total de unidades (traits).}

\item{name}{[EN] Name of the unit (default \code{"trait"}).\cr
[ESP] Nombre de la unidad (por defecto \code{"trait"}).}
}
\value{
[EN] Progress bar ID (integer, returned by \code{cli}).\cr
[ESP] ID de la barra de progreso (entero, devuelto por \code{cli}).
}
\description{
[EN] Returns the ID of a \code{cli} progress bar ready to be updated with
\code{\link[=gh_progress_tick]{gh_progress_tick()}} and closed with \code{\link[=gh_progress_done]{gh_progress_done()}}.
}
\details{
[ESP] Devuelve el ID de una barra de progreso \code{cli} lista para
actualizarse con \code{\link[=gh_progress_tick]{gh_progress_tick()}} y cerrarse con \code{\link[=gh_progress_done]{gh_progress_done()}}.
}
\examples{
\dontrun{
pb <- gh_progress_init(100)
for (i in seq_len(100)) {
  Sys.sleep(0.01)
  gh_progress_tick(pb, paste("trait", i))
}
gh_progress_done(pb)
}
}
