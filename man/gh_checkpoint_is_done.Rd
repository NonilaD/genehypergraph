% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_utils.R
\name{gh_checkpoint_is_done}
\alias{gh_checkpoint_is_done}
\title{Check whether a trait is already done in the checkpoint}
\usage{
gh_checkpoint_is_done(config, trait_id)
}
\arguments{
\item{config}{[EN] Configuration list returned by \code{\link[=load_config]{load_config()}}.\cr
[ESP] Lista de configuracion devuelta por \code{\link[=load_config]{load_config()}}.}

\item{trait_id}{[EN] EFO/MONDO identifier of the trait.\cr
[ESP] Identificador EFO/MONDO del trait.}
}
\value{
[EN] \code{TRUE} if the trait is already completed, \code{FALSE}
otherwise.\cr
[ESP] \code{TRUE} si el trait ya esta completado, \code{FALSE} en caso contrario.
}
\description{
[EN] Returns \code{TRUE} if \code{trait_id} is present in the checkpoint with
\code{status == "ok"}. Convenience wrapper around \code{\link[=gh_checkpoint_load]{gh_checkpoint_load()}}.
}
\details{
[ESP] Devuelve \code{TRUE} si \code{trait_id} esta en el checkpoint con
\code{status == "ok"}. Wrapper conveniente sobre \code{\link[=gh_checkpoint_load]{gh_checkpoint_load()}}.
}
\examples{
\dontrun{
config <- load_config()
if (gh_checkpoint_is_done(config, "EFO_0001645")) {
  message("Skipping already completed trait.")
}
}
}
